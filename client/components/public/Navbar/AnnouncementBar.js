export default function AnnouncementBar() {
  return (
    <div
      style={{ background: "var(--pub-accent)", color: "#fff" }}
      className="w-full py-1.5 px-4 flex justify-center items-center text-center"
    >
      <div className="text-xs md:text-sm font-medium tracking-[0.15em] uppercase">
        SBELTIC • OBTÉN 10% AL REFERIR A UN AMIGO
      </div>
    </div>
  );
}
